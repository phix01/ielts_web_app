package com.ielts.repository;

/**
 * BookRepository removed - books are served from classpath resources.
 * Keep this placeholder to avoid compilation problems while migrating other code.
 */
public interface BookRepository {
}
