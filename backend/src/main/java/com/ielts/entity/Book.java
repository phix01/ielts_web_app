package com.ielts.entity;

/**
 * Book entity removed. Previously this was a JPA entity mapping to the books table.
 * Books are now served from classpath resources and this class is intentionally left empty
 * to avoid accidental usage. Delete this file once the codebase no longer references it.
 */
public final class Book {
    private Book() {}
}
